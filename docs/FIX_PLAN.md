# FIX_PLAN

This file was an inventory of 61 findings from early 2026. Most of it is **paid or false**.

Do not re-open embeddings, admin JWT, SSRF, magic-link atomicity, Stripe idempotency, or `sync-core` engine from this document.

Living board: [`NOW.md`](./NOW.md).

Open work (PRs against `develop`):

- #478 session + magic-link origin
- #479 sync envelope + cursors + soft-delete
- #480 plugin zip-slip + host allowlist

Stale audits: `docs/archived/`.
